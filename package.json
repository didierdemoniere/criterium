{
  "name": "criterium",
  "version": "0.3.0",
  "description": "filtering, sorting and paginate everything with mongo-like syntax",
  "scripts": {
    "build": "pnpm --stream -r run build",
    "dev": "pnpm --parallel -r run dev",
    "test": "pnpm --parallel -r run test",
    "test:dev": "pnpm --parallel -r run test:dev"
  },
  "author": "",
  "license": "ISC"
}
